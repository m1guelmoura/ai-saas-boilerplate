# ============================================
# AI SaaS BOILERPLATE - ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env.local and fill in your actual values
# cp .env.example .env.local

# ============================================
# SUPABASE CONFIGURATION
# ============================================
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api

# Your Supabase project URL
# Find it at: Supabase Dashboard → Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Your Supabase anon/public key (safe to expose to client)
# Find it at: Supabase Dashboard → Settings → API → Project API keys → anon public
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Your Supabase service role key (NEVER expose to client!)
# Find it at: Supabase Dashboard → Settings → API → Project API keys → service_role
# ⚠️ CRITICAL: This key bypasses Row Level Security. Keep it secret!
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# ============================================
# APP CONFIGURATION
# ============================================
# Your application URL (for redirects and callbacks)
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# STRIPE CONFIGURATION
# ============================================
# Get these from: https://dashboard.stripe.com/apikeys

# Stripe Secret Key (server-side only)
# Find it at: Stripe Dashboard → Developers → API keys → Secret key
# Development: starts with sk_test_
# Production: starts with sk_live_
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here

# Stripe Publishable Key (client-side safe)
# Find it at: Stripe Dashboard → Developers → API keys → Publishable key
# Development: starts with pk_test_
# Production: starts with pk_live_
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key_here

# Stripe Webhook Secret (for webhook signature verification)
# Find it at: Stripe Dashboard → Developers → Webhooks → Click on your endpoint → Signing secret
# Development (local): Run `stripe listen --forward-to localhost:3000/api/stripe/webhook` to get a whsec_ value
# Production: Get it from your webhook endpoint settings
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# ============================================
# STRIPE PRICE IDs (SUBSCRIPTION PLANS)
# ============================================
# Create products and prices in Stripe Dashboard, then copy the Price IDs here
# Find them at: Stripe Dashboard → Products → Click on product → Pricing section

# Starter Plan - Monthly
NEXT_PUBLIC_STRIPE_PRICE_ID_STARTER_MONTHLY=price_xxxxx

# Professional Plan - Monthly
NEXT_PUBLIC_STRIPE_PRICE_ID_PROFESSIONAL_MONTHLY=price_xxxxx

# Enterprise Plan - Monthly
NEXT_PUBLIC_STRIPE_PRICE_ID_ENTERPRISE_MONTHLY=price_xxxxx

# Starter Plan - Yearly
NEXT_PUBLIC_STRIPE_PRICE_ID_STARTER_YEARLY=price_xxxxx

# Professional Plan - Yearly
NEXT_PUBLIC_STRIPE_PRICE_ID_PROFESSIONAL_YEARLY=price_xxxxx

# Enterprise Plan - Yearly
NEXT_PUBLIC_STRIPE_PRICE_ID_ENTERPRISE_YEARLY=price_xxxxx

# ============================================
# GOOGLE OAUTH (Optional - if using Google OAuth)
# ============================================
# Configure Google OAuth in Supabase Dashboard:
# Supabase Dashboard → Authentication → Providers → Google
# Then add your Google Client ID and Secret there (not here)

# ============================================
# NOTES
# ============================================
# - Never commit .env.local to version control
# - Always use test keys (sk_test_, pk_test_) in development
# - Switch to live keys (sk_live_, pk_live_) only in production
# - Keep SUPABASE_SERVICE_ROLE_KEY and STRIPE_SECRET_KEY secret at all times
